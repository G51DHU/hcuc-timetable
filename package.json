{
  "name": "hcuc-timetable",
  "version": "0.1.0",
  "description": "This is the frontend for hcuc-timetable.",
  "license": "MIT",
  "scripts": {
    "start": "vite --host",
    "build": "vite build",
    "wbuild": "vite build --watch",
    "preview": "vite preview --host --port 3000",
    "lint": "npx standard",
    "fix": "npx standard --fix"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^1.1.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.3.0"
  },
  "devDependencies": {
    "babel-preset-nano-react-app": "^0.1.0",
    "standard": "^17.0.0",
    "vite": "^2.6.7"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ]
  }
}
